import { setup } from './setup.util'
import { uploadDocument } from './upload-document.util'
import { getDocument } from './get-document.util'

export const cloud = { 
    setup,
    uploadDocument,
    getDocument
}
